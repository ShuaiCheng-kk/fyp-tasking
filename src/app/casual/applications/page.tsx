// Casual Worker's Applications page — a Casual Worker is a confirmed Guest User who can still
// apply to other jobs, so this is the exact same page/component as Guest's Applications tab
// (same API routes, which are role-agnostic), not a rebuild.
export { default } from '@/app/guest/applications/page'
